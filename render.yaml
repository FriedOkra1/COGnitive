services:
  - type: web
    name: cognitive-backend
    runtime: node
    buildCommand: cd backend && npm install && npm run build
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
      - key: FRONTEND_URL
        value: https://cog-nitive.vercel.app
    healthCheckPath: /health
    autoDeploy: true

